Use the build-agent skill to create an agent `ws-refund-agent-triage` in path `orq-workshop/workshop`, a copy of `ws-refund-agent-delegating` (same instructions, model and function tools by key: `ws-lookup-order`, `ws-get-policy`, `ws-issue-refund`) with one change: the `advisor` tool moves from `openai/gpt-5.6-sol` to `anthropic/claude-sonnet-5` and `max_uses` drops to 1. Then invoke both agents through the Responses API with the input "Refund ord_a6 please, the frame arrived scratched.", executing the `function_call` items with `app.refund_agent.tools.dispatch` and continuing with `previous_response_id` until each answers. Use the orq MCP tools (`list_spans`) on the last trace of each run and give me a two-row table: agent, advisor model, advisor span cost, agent's own model cost, total, and whether the advice changed the decision.
