Add a fallback chain and a 600 second exact-match cache to `modules/01-gateway/run.py` without changing anything under `app/`. The primary model is `openai/gpt-4.1` with a 900 ms call timeout, fallbacks are `openai/gpt-4.1-nano` then the model from `.env`. Run the script. Then use the orq MCP tools (`list_traces`, `list_spans`) to fetch the spans of the fallback trace and tell me which model actually answered and what it cost.
