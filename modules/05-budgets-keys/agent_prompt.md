Use the orq MCP tool `query_analytics` to break down spend by model for the last 24 hours in this workspace. Give me a table with model, requests and cost, name the most expensive model, and propose one budget for it: scope (model, provider, identity or API key), period, amount in USD and a requests-per-minute value, with the exact `orq budgets create` command I would run with a Management Key. Do not create it. If `query_analytics` cannot group by model, say so and use `list_traces` over the same window instead.
