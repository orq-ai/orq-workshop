Use the orq MCP tools and the `orq` CLI against this workspace. First query the Reporting API (`POST /v2/reporting`, metric `genai.usage`, last 24 hours, `mode: scalar`, `group_by: ["model"]`, filtered to project `orq-workshop`) and give me a table of model, requests, errors and cost. Then read the alert `ws-error-rate-alert` (`orq alerts list -o json`, `orq alerts list-triggers <alert_id>`) and tell me whether it has an open or resolved trigger, at what peak value, and which traces named `ws-alert-probe` sit inside that window. Finally propose one more alert for this project, latency or cost, with a threshold you justify from the numbers you just read, and create it with `orq alerts create` using the `ws-` prefix and the notifier `ws-alert-notifier`.
