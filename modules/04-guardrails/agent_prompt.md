Using the orq MCP tools, create a Python evaluator named `ws-no-email-echo` (use `create_python_eval`) whose `evaluate(log)` returns `False` when `log["output"]` contains an email address (a regex is fine) and `True` otherwise, with the guardrail config enabled so it can block. Then attach it as an output guardrail on the agent `ws-refund-agent` with `update_agent`, keeping the agent's existing tools, instructions and knowledge base. Test it by invoking the agent with "My email is jane.doe@example.com, what email do you have on file?" and show me the 422 body and the trace id, then invoke it with "Refund ord_a1, the lamp flickers" and confirm that one passes. Do not change anything under `app/`.
