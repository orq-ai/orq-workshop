Use the analyze-trace-failures skill on the traces tagged `traffic` from the last hour (they carry `metadata.variant` and `metadata.expected`; the search API has no `tags` filter, so filter on `metadata.variant` and check `orq.tags` on each trace). Read at least 30 of them, write freeform notes, then group them into 2 to 4 named failure modes with a Pass/Fail definition each. Then use build-evaluator for the top failure mode: propose a binary judge prompt, create it with `create_llm_eval` under the key `ws-<failure-mode>-judge`, and explain how you would validate it with TPR/TNR. Finally use run-experiment to compare the agents `ws-refund-agent` and `ws-refund-agent-vulnerable` on the dataset `ws-refund-eval` with your new judge and `ws-refund-policy-judge`, and report the per-agent pass rates.
