Use the orq MCP tools on this workspace. List the items of the annotation queue `ws-review-queue` and, for each one that has no review yet, fetch the span, read the customer message and the agent's answer, and decide `rating` good or bad against the refund policy in `app/data/kb/*.md` (a refusal to explain the policy, an invented exception, or a refund outside the window is bad; add a `defects` value from the workspace's fixed list where one fits). Apply the annotations with the Annotations API. Then give me a table: trace id, rating, defect, one-line reason. Do not remove anything from the queue and do not touch the dataset.
