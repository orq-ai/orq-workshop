Create a smart router called `ws-refund-router` with the orq MCP tools or the `orq smart-routers` CLI (it may already exist; reuse it) over `openai/gpt-4o-mini`, `openai/gpt-4.1-mini`, `openai/gpt-4.1` and `anthropic/claude-haiku-4-5-20251001` with the COST profile. Send 5 refund questions of mixed difficulty to its `model_ref` through the gateway (`app.refund_agent.agent.chat(text, model=<model_ref>)`), then use `query_analytics` to compare the cost of those 5 calls against 5 identical calls sent straight to `openai/gpt-4.1`. Report cost per call, which model the router picked for each, and whether COST or QUALITY would be the right profile for a refund desk. Do not change anything under `app/`.
