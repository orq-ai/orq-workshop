"""Refund agent sample app used by every workshop module.

The library never changes across modules. Each module composes it through
`extra_body`, environment flags, or by swapping the model for a managed agent.
"""
